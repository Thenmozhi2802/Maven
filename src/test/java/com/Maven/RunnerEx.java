package com.Maven;

import java.io.IOException;

import org.openqa.selenium.WebDriver;
import org.testng.annotations.Test;

public class RunnerEx extends Base{
    
	                //public static void main(String[] args) throws IOException {
		//Runner a=new Runner();
		            //WebDriver driver=browserLaunch(dataEx("Sheet1", 1, 0));
		//driver1.findElement(By.id("email")).sendKeys("Hello");
		//or use return type
		//WebElement userbox=driver.findElement(By.id("email"));
		//KeyToSend(userbox,"Hello");
		//WebElement passwordbox=driver.findElement(By.id("pass"));
		//KeyToSend(passwordbox,"123");
		//KeyToSend(userbox,"Hello123"); if it is not working then create another webelement and sendtokeys
		//WebElement userbox1=driver.findElement(By.id("email"));
		//KeyToSend(userbox1,"Hello123");
		            //Pom a=new Pom(driver);
	                //KeyToSend(a.getUserName(),dataEx("Sheet1", 1, 1));
		            //KeyToSend(a.getPassword(),dataEx("Sheet1", 1, 2));
		            //click(a.getPassword());
                   	//              	}
                    //  }
	                             
	                       //@Test
	                       //private void testcase1() throws IOException {
		                   //WebDriver driver=browserLaunch(dataEx("Sheet1", 1, 0));
                           //Pom a=new Pom(driver);
                           //KeyToSend(a.getUserName(),dataEx("Sheet1", 1, 1));
                           //KeyToSend(a.getPassword(),dataEx("Sheet1", 1, 2));
                           //click(a.getPassword());
	@Test
	   private void methodB() {
		System.out.println("B");
		}
	@Test
	   private void methodC() {
		System.out.println("C");
        }
	@Test
	   private void methodA() {
		System.out.println("A");

	}

	}
	
	
	
	
	
	
	
	
	
	
